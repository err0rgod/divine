# Aider integration

The package exposes `divine-aider` so automation can detect a consistent entry point. It fails
clearly unless Aider is installed and its installed version supports a reliable isolated base-URL
override.

Aider was not installed in the build environment, so no wrapper was generated from an assumed
schema and no compatibility claim is made. Installing an unrelated global CLI is outside Divine
Router's setup process.
