name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test-matrix:
    name: Tests (${{ matrix.os }}, Python ${{ matrix.python }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python: ["3.12", "3.13"]
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python }}
          cache: pip
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -e ".[test]"
      - run: pytest -q

  quality:
    name: Quality, coverage, package, docs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: "3.13"
          cache: pip
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -e ".[test,docs]"
      - run: ruff format --check src tests migrations
      - run: ruff check src tests migrations
      - run: mypy src/divine_router
      - name: Core coverage gate
        run: >-
          pytest -q
          --cov=divine_router.protocols
          --cov=divine_router.routing
          --cov=divine_router.reliability
          --cov-report=term-missing
      - run: python -m build
      - run: mkdocs build --strict
      - name: Tracked-file secret heuristics
        shell: bash
        run: |
          pattern="(sk-[A-Za-z0-9_-]{20,}|gh[pousr]_[A-Za-z0-9_]{20,}|github_pat_[A-Za-z0-9_]{20,}|AIza[A-Za-z0-9_-]{30,}|AKIA[A-Z0-9]{16}|xox[baprs]-[A-Za-z0-9-]{20,}|rnd_[A-Za-z0-9_-]{20,})"
          if git grep -nEI "$pattern" -- ':!uv.lock' ':!Agents.md'; then
            echo "Potential committed credential detected" >&2
            exit 1
          fi

  docker:
    name: Docker build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: docker/setup-buildx-action@v4
      - uses: docker/build-push-action@v7
        with:
          context: .
          push: false
          tags: divine-router:ci
